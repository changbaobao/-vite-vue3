import request from '@/utils/request'
/**
 *
 * @param {查询用户列表} params
 */
export function getAccountData(params) {
    return request({
        url: '',
        method: 'post',
        params
    })
}